<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.etjava.mapper.FrameworkMapper">

    <select id="findByMenuId" parameterType="Integer" resultType="Framework">
        select * from t_framework where treeId=#{menuId}
    </select>
</mapper>